import React from 'react'
import { BrowserRouter as Router, Switch, Route } from 'react-router-dom'

import about

const routes = () => (
    <Router>
        <Switch>
            <Route exact path
        </Switch>
    </Router>
)